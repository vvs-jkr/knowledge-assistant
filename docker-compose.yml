services:
    api:
        build:
            context: ./apps/api
        ports:
            - "8080:8080"
        env_file: .env
        volumes:
            - ./data:/app/data

    web:
        build:
            context: ./apps/web
        ports:
            - "3000:80"
        depends_on:
            - api
